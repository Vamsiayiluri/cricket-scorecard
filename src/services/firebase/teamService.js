/**
 * Placeholder for future dedicated team entities.
 * Teams are currently embedded inside match documents.
 */
import { COLLECTIONS } from "./constants";

export const TEAMS_COLLECTION = COLLECTIONS.TEAMS;

export const listTeams = async () => {
  // Reserved for Phase D+ when teams become first-class documents.
  return [];
};
