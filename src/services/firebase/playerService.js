/**
 * Placeholder for future dedicated player entities.
 * Players are currently embedded inside match team arrays.
 */
import { COLLECTIONS } from "./constants";

export const PLAYERS_COLLECTION = COLLECTIONS.PLAYERS;

export const listPlayers = async () => {
  // Reserved for Phase D+ when players become first-class documents.
  return [];
};
